name: AutoTest Search Cruise

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress run
        id: cypress
        uses: cypress-io/github-action@v6
        with:
          # Указываем путь к файлу теста. 
          # Если файл лежит в папке cypress.Search.Cruise, путь должен быть таким:
          spec: cypress.Search.Cruise/cypress/e2e/monitoring/cruise.cy.js
        env:
          LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}
        # Оставляем как в Авиа, чтобы бот всегда отправлял отчет
        continue-on-error: true

      - name: Read Offers Count
        if: always()
        run: |
          # Ищем файл offers_count.txt по всему репозиторию
          FILE=$(find . -name "offers_count.txt" | head -n 1)
          if [ -n "$FILE" ] && [ -f "$FILE" ]; then
            VALUE=$(cat "$FILE")
            echo "OFFERS_COUNT=$VALUE" >> $GITHUB_ENV
          else
            echo "OFFERS_COUNT=0" >> $GITHUB_ENV
          fi

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }} 
          format: markdown
          message: |
            ${{ github.event.inputs.user_chat_id != '' && '👤 **Manual Testing**' || '📢 **Hourly Testing**' }}
            
            🚢 **Cruise Search Result**
            **Status:** ${{ steps.cypress.outcome == 'success' && '✅ Success' || '❌ Error' }}
            **Offers Found:** ${{ env.OFFERS_COUNT || '0' }}
            **Time:** ${{ github.event_name == 'schedule' && 'Hourly Testing ⏰' || 'Manual Testing 🔘' }}