name: Cruise AutoTest

on:
  schedule:
    # Запуск в 15 минут каждого часа, чтобы не пересекаться с ЖД
    - cron: '15 * * * *'
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress run
        id: cypress
        uses: cypress-io/github-action@v6
        with:
          # Работаем из корня, так как package.json на верхнем уровне
          working-directory: . 
          # ИСПРАВЛЕНО: путь к файлу теста именно для КРУИЗОВ
          spec: cypress/e2e/monitoring/cruise.cy.js 
          browser: chrome
        env:
          # Секреты для авторизации
          CYPRESS_LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          CYPRESS_LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}

      - name: Extract Results
        id: extract_results
        if: always() 
        run: |
          # Проверяем наличие файла с количеством найденных круизов
          if [ -f "offers_count.txt" ]; then
            COUNT=$(cat "offers_count.txt")
            echo "count=$COUNT" >> $GITHUB_OUTPUT
          else
            echo "count=0" >> $GITHUB_OUTPUT
          fi

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }} 
          format: markdown
          message: |
            ${{ github.event.inputs.user_chat_id != '' && '👤 **Manual Testing**' || '📢 **Hourly Testing**' }}
            
            🚢 **Cruise Search Result**
            **Status:** ${{ steps.cypress.outcome == 'success' && '✅ Success' || '❌ Error' }}
            **Cruises Found:** ${{ steps.extract_results.outputs.count }}
            **Mode:** ${{ github.event_name == 'schedule' && 'Scheduled ⏰' || 'Manual 🔘' }}