name: Cruise AutoTest

on:
  schedule:
    - cron: '0 * * * *' 
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Cypress Tests
        id: cypress
        uses: cypress-io/github-action@v6
        with:
          # ВАЖНО: Указываем ту папку, в которой лежит package.json
          # Судя по логам, это первая папка в твоем репозитории
          working-directory: cypress.Search.Cruise
          # Если проект вложен еще глубже, Cypress найдет его через параметр project
          project: ./cypress.Search.Cruise
          spec: cypress/e2e/monitoring/cruise.cy.js
          browser: chrome
        env:
          LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}

      - name: Extract Results
        id: extract_results
        if: always()
        run: |
          # Используем поиск по всему дереву, чтобы найти файл с результатом
          FILE=$(find . -name "offers_count.txt" | head -n 1)
          if [ -n "$FILE" ] && [ -f "$FILE" ]; then
            COUNT=$(cat "$FILE")
            echo "count=$COUNT" >> $GITHUB_OUTPUT
          else
            echo "count=0" >> $GITHUB_OUTPUT
          fi

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }} 
          format: markdown
          message: |
            ${{ github.event.inputs.user_chat_id != '' && '👤 **Manual Testing**' || '📢 **Hourly Testing**' }}
            
            🚢 **Cruise Search Result**
            **Status:** ${{ steps.cypress.outcome == 'success' && '✅ Success' || '❌ Error' }}
            **Offers Found:** ${{ steps.extract_results.outputs.count || '0' }}
            **Mode:** ${{ github.event_name == 'schedule' && 'Hourly ⏰' || 'Manual 🔘' }}